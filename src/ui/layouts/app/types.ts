import type { ReactNode } from 'react'

/**
 * Interface representing the properties for the AppLayout component.
 */
export interface AppLayoutProps {
  /**
   * The content to be rendered within the AppLayout component.
   */
  children: ReactNode
}
